/* @flow strict */
import * as icons from './icons';
import { IconSize } from './Size';
import { IconButton } from './Button';

export const Icon = {
  icons,
  Size: IconSize,
  Button: IconButton,
};

export * from './types';
